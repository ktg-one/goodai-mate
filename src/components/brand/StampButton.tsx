'use client';

/**
 * Re-export of the canonical production StampButton (awwwwards-ui-skills).
 * Kept for any legacy brand/ imports. Prefer direct: import StampButton from '@/components/StampButton'
 */
export { default as StampButton } from '../StampButton';
export type { StampButtonProps } from '../StampButton';
