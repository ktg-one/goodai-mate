## CURRENT AGENT CONTEXT (post-refactor)

The site was refactored after the original foundation phase into the current Voice Agent hero + full mechanical brutalist mail-board (ribbons, non-uniform pinned dockets, in-tray, footer ritual).

Development uses:
- Local skills from .agents/skills + .claude/skills (goodai-award-configuration + goodai-agent-team for craft; gsd-health, gsd-docs-update, gsd-progress etc. for planning/docs hygiene — added 2026-06-04).
- PRODUCT.md + public/ as SSOT.
- gsd for .planning/ (old 01-05 phases archived to milestones/v0.9-pre-refactor-phases/).

Next agent work should:
1. Run `gsd health` / `gsd progress` at start.
2. For UI/animation craft: use goodai-award-configuration (5 agents) + optional gsd planner synthesis.
3. For any planning/docs drift: gsd-docs-update --verify-only then --force as needed.
4. Verify 60fps, stamp physics (90-160ms), one-red, WONK, reduced-motion static fidelity, brand voice, and that docs/.planning match the live refactored site.

## Historical Init Agent Summary — 2026-04-01 (pre-refactor)

### What was done (original)

1. Read all specs — inbox/app-spec.md, inbox/app-spec-ui.md, .planning/PROJECT.md.
2. Created feature_list.json — 251 end-to-end tests (old shader/landing/chat/lead/cursor scope).
3. Created init.ps1.
4. No source at that point.

### What happened later

The original 5-phase plan (shader visual → chat → lead) was superseded by the award-refactored Voice + docket site. Old tests, inbox/, and phase plans are no longer active (archived).

## Old "NEXT AGENT" note (historical)

The next agent to run is the Coding Agent at: .claude/agents/coding_agent.md
It will read feature_list.json... (obsolete — use gsd + award skills + current PRODUCT.md instead).

Specs were at: inbox/app-spec.md ... (archived / superseded).

GSD planning docs at: .planning/ (still used, but content updated 2026-06-04).

Init script: init.ps1 (may still be useful for fresh clones).

---

*Updated 2026-06-04 with gsd-health skills add + docs alignment for the refactored Good'ai site.*
[impeccable 2026-06-04] DESIGN FIDELITY AUDIT + PATCHES MILESTONE COMPLETE.
Gate0: npx skills list (impeccable + goodai-award + gsap* + awwwards*), context.mjs, brand/audit/animate/overdrive refs, all design files (Brutalist Skill.html wave/stamp/WONK, colors, ui_kits, preview, assets, PRODUCT, scope).
Screenshots: playwright audit-*-clean / -final / design-cards; read_file visuals + tall crops.
Dev: multiple clean (kills, .next, detached), 3000 stable, 500s from syntax fixed in loop.
DESIGN-GAPS.md created (9 categories, pre/post status, shared for swarm).
Patches (surgical, every line traces to request/scope, read-first before write, match style, no speculative):
  globals.css: prepended full SSOT tokens/hl/shadows from public/colors_and_type.css + stamp-btn full (120ms press, variants, one red, focus) + ribbon png verbatim + enhanced overlays + reduced complete static (perfs/pins/rots/letters visible).
  HomeClient.tsx: integrated letter-a/good/i/swan.svg as micro docket stamps (corners, filed in-tray) to fix under-use; removed '01' nums from THE DOCKET (brand register ban); GSAP flutter amp + comments for mechanical ratchet; static reduced enhanced.
  VoiceAgentHero.tsx: added wave-ribbon.png layer inside stamp-box visualizer (design signal per Skill.html, mix-blend-multiply, scaleY(0.7), filing var shift) + clean full working (no dups, descent, stamp, red one).
Verify loop: tsc clean, dev ready no error, final screenshots show wave ribbon in hero box, png in mail-ribbon, dockets clean titles + letters code, stamp red, wonk footer, paper/stamp shadows.
Milestones: Gate0, explore, screenshots (pre/post), audit, gaps-doc, patches, verify. All in todo + thoughts.
