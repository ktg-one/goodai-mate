I will define `.gai-input` globally in `globals.css` with brutalist stylings. Then I will log this change in `public/log.md`.

Plan:
1. Edit `src/app/globals.css` to add the `.gai-input` class and its pseudo-classes (`:focus-visible`, `::placeholder`, `:disabled`). I will insert it right before the Buttons section.
2. Verify the changes using `grep`.
3. Check `public/log.md` to sequentially tick the implemented area. I'll add `- [x] Defined cohesive brutalist .gai-input globally with strict physics and focus states.` at the end of the file.
4. Execute pre-commit instructions.
