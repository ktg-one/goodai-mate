---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - src/types/glsl.d.ts
  - src/shaders/test.frag
  - src/app/globals.css
  - src/app/layout.tsx
  - package.json
  - package-lock.json
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03, FOUND-04]

must_haves:
  truths:
    - "A .glsl/.frag/.vert file can be imported in a TypeScript component without type errors"
    - "GLSL imports return string content in both dev (Turbopack) and prod (webpack) builds"
    - "Perth Disruptor palette colors are available as Tailwind classes (e.g., bg-background, text-accent) and CSS custom properties (e.g., var(--accent))"
    - "DM Sans renders as the body font and JetBrains Mono renders as the mono font with zero layout shift"
  artifacts:
    - path: "next.config.ts"
      provides: "Dual GLSL loader config (turbopack.rules + webpack raw-loader)"
      contains: "turbopack"
    - path: "src/types/glsl.d.ts"
      provides: "TypeScript module declarations for .glsl, .frag, .vert"
      contains: "declare module"
    - path: "src/shaders/test.frag"
      provides: "Minimal GLSL fragment shader for pipeline verification"
      contains: "gl_FragColor"
    - path: "src/app/globals.css"
      provides: "Perth Disruptor palette as CSS custom properties and Tailwind theme tokens"
      contains: "--accent"
    - path: "src/app/layout.tsx"
      provides: "DM Sans + JetBrains Mono font loading via next/font/google"
      contains: "DM_Sans"
  key_links:
    - from: "next.config.ts"
      to: "src/shaders/test.frag"
      via: "turbopack.rules raw type + webpack raw-loader"
      pattern: "turbopack.*rules.*glsl|raw-loader"
    - from: "src/types/glsl.d.ts"
      to: "src/shaders/test.frag"
      via: "TypeScript module resolution"
      pattern: "declare module.*\\.frag"
    - from: "src/app/globals.css"
      to: "src/app/layout.tsx"
      via: "CSS import and font variable assignment"
      pattern: "--font-sans.*--font-dm-sans"
---

<objective>
Set up the GLSL import pipeline, Perth Disruptor design tokens, and brand fonts so that all subsequent phases can build components against the correct palette, typography, and shader infrastructure.

Purpose: Phase 1 is a hard prerequisite -- GLSL loading failure blocks Phase 2 entirely, and wrong design tokens mean every component is built to the wrong palette.
Output: Working next.config.ts with dual GLSL loader, TypeScript declarations, design tokens in CSS/Tailwind, DM Sans + JetBrains Mono fonts loaded.
</objective>

<execution_context>
@C:/Users/kevin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kevin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GLSL pipeline — dual loader config, TypeScript declarations, and dependency install</name>
  <files>next.config.ts, src/types/glsl.d.ts, src/shaders/test.frag, package.json</files>
  <action>
1. Install dependencies:
   ```
   npm install three ai @ai-sdk/openai-compatible
   npm install -D raw-loader @types/three
   ```

2. Update `next.config.ts` with BOTH Turbopack and webpack GLSL configs. This dual config is critical -- Turbopack runs in dev (`next dev`), webpack runs in prod (`next build`). The current file is empty config.

   ```typescript
   import type { NextConfig } from "next";

   const nextConfig: NextConfig = {
     turbopack: {
       rules: {
         '*.glsl': { loaders: ['raw-loader'], as: '*.js' },
         '*.vert': { loaders: ['raw-loader'], as: '*.js' },
         '*.frag': { loaders: ['raw-loader'], as: '*.js' },
       },
     },
     webpack: (config) => {
       config.module.rules.push({
         test: /\.(glsl|vert|frag)$/,
         use: 'raw-loader',
       });
       return config;
     },
   };

   export default nextConfig;
   ```

3. Create `src/types/glsl.d.ts` with module declarations:
   ```typescript
   declare module '*.glsl' {
     const value: string;
     export default value;
   }
   declare module '*.vert' {
     const value: string;
     export default value;
   }
   declare module '*.frag' {
     const value: string;
     export default value;
   }
   ```

4. Create `src/shaders/test.frag` -- minimal fragment shader for pipeline verification:
   ```glsl
   precision mediump float;
   uniform float uTime;
   void main() {
     gl_FragColor = vec4(0.847, 0.416, 0.239, 1.0); // #D86A3D Silicon Copper
   }
   ```

5. Verify the pipeline works by creating a temporary test: in the dev server, confirm that importing `test.frag` returns a string (not `[object Module]`). The executor should run `npm run dev` and check the build succeeds without errors, then run `npm run build` to confirm webpack path also works.

IMPORTANT: Do NOT use `experimental.turbo` -- that key was removed in Next.js 16. The correct key is `turbopack.rules` at the top level of NextConfig.
  </action>
  <verify>
    <automated>cd "C:/Users/kevin/knowledge2026/Projects-Coding/nextjs" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `npm run build` completes without errors
    - `src/types/glsl.d.ts` exists with declarations for .glsl, .frag, .vert
    - `src/shaders/test.frag` exists and contains valid GLSL
    - `next.config.ts` has both `turbopack.rules` and `webpack` config for raw-loader
    - `three`, `@types/three`, `ai`, `@ai-sdk/openai-compatible`, `raw-loader` are in package.json
  </done>
</task>

<task type="auto">
  <name>Task 2: Perth Disruptor design tokens and brand fonts</name>
  <files>src/app/globals.css, src/app/layout.tsx</files>
  <action>
1. Replace `src/app/globals.css` entirely. Remove the existing Geist font references and light/dark media query. The site is always dark -- no light mode. Set up:

   - CSS custom properties for Perth Disruptor palette:
     - `--background: #0C0C0C` (Deep Tech Onyx)
     - `--foreground: #D7D2CB` (Cottesloe Sand -- body text)
     - `--accent: #D86A3D` (Silicon Copper)
     - `--accent-soft: rgba(216, 106, 61, 0.35)` (accent at 0.35 opacity for subtle use)
     - `--heading: #EDE9E3` (heading text, slightly brighter than body)
     - `--surface: #1A1A1A` (card/container background)
     - `--surface-raised: #242424` (elevated surface)
     - `--border: rgba(215, 210, 203, 0.1)` (subtle borders)

   - Tailwind v4 `@theme inline` block mapping these to Tailwind classes:
     - `--color-background: var(--background)`
     - `--color-foreground: var(--foreground)`
     - `--color-accent: var(--accent)`
     - `--color-accent-soft: var(--accent-soft)`
     - `--color-heading: var(--heading)`
     - `--color-surface: var(--surface)`
     - `--color-surface-raised: var(--surface-raised)`
     - `--color-border: var(--border)`
     - `--font-sans: var(--font-dm-sans)`
     - `--font-mono: var(--font-jetbrains-mono)`

   - Body styles: `background: var(--background)`, `color: var(--foreground)`, `font-family: var(--font-sans), sans-serif`

   - Remove the `@media (prefers-color-scheme: dark)` block entirely -- this site is always dark.

2. Update `src/app/layout.tsx`:
   - Replace `Geist` and `Geist_Mono` imports with `DM_Sans` and `JetBrains_Mono` from `next/font/google`
   - DM_Sans: subsets `['latin']`, weight `['300', '400', '500', '600', '700']`, variable `'--font-dm-sans'`
   - JetBrains_Mono: subsets `['latin']`, weight `['300', '400']`, variable `'--font-jetbrains-mono'`
   - Update the html className to use the new font variables
   - Update metadata: title `"Good'ai — Business Automations, Sorted"`, description `"Perth-based business automations for SMEs. Tell us your problem, we'll figure out how to fix it."`
   - Keep `lang="en"`, `h-full`, `antialiased` on html
   - Keep `min-h-full flex flex-col` on body
  </action>
  <verify>
    <automated>cd "C:/Users/kevin/knowledge2026/Projects-Coding/nextjs" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `globals.css` contains all 8 Perth Disruptor CSS custom properties (--background through --border)
    - `globals.css` has @theme inline block mapping all colors and fonts to Tailwind
    - No light mode / prefers-color-scheme media query exists
    - `layout.tsx` loads DM_Sans (weights 300-700) and JetBrains_Mono (weights 300-400) via next/font/google
    - `layout.tsx` metadata reflects Good'ai branding
    - `npm run build` succeeds
    - Font CSS variables (--font-dm-sans, --font-jetbrains-mono) are applied to the html element
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full Phase 1 success criteria:

1. GLSL pipeline: Create a temporary test page that imports `test.frag` and renders its content in a `<pre>` tag. Confirm the import resolves to a string containing `gl_FragColor`, not `[object Module]` or undefined. Test in both `npm run dev` (Turbopack) and `npm run build` (webpack).

2. Design tokens: Inspect the rendered page -- background should be #0C0C0C, text should be Cottesloe Sand (#D7D2CB). Confirm `bg-accent` produces #D86A3D. Confirm `text-heading` produces #EDE9E3.

3. Fonts: Inspect the rendered page -- body text should render in DM Sans, any `font-mono` element should render in JetBrains Mono. No layout shift (CLS = 0) on load.

4. TypeScript: Open any .ts file, type `import shader from '../shaders/test.frag'` and confirm `shader` is typed as `string` with no red squiggles.
</verification>

<success_criteria>
- `npm run build` completes with zero errors
- `npm run dev` starts without errors
- .glsl/.frag/.vert imports resolve to strings in both Turbopack and webpack
- TypeScript provides autocomplete for shader imports without errors
- Perth Disruptor palette is available as both CSS vars and Tailwind classes
- DM Sans and JetBrains Mono load via next/font with zero CLS
- Page background is #0C0C0C, body text is #D7D2CB
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
