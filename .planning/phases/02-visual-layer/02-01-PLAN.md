---
phase: 02-visual-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .env.example
  - src/app/layout.tsx
  - src/components/HeroSection.tsx
  - public/assets/logo-mark.svg
  - public/fonts/Fraunces-VariableFont_SOFT_WONK_opsz_wght.ttf
  - public/fonts/Fraunces-Italic-VariableFont_SOFT_WONK_opsz_wght.ttf
autonomous: true
requirements: [SHDR-01, SHDR-02, SHDR-03, SHDR-04, SHDR-05, SHDR-06, SHDR-07, SHDR-08, CURS-01, CURS-02, CURS-03, CURS-04, CURS-05, CURS-06, LAND-01]

must_haves:
  truths:
    - "Stale shader/cursor planning is explicitly superseded by the current paper-brand direction"
    - "HeroSection no longer references a missing brand asset"
    - "Font loading no longer depends on zero-byte files"
    - "NEXT_PUBLIC_WEB3FORMS_KEY is documented in .env.example"
    - "Changes are surgical and preserve unrelated dirty worktree edits"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 2 scope that matches the current Next site pivot"
      contains: "paper-brand"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirement status notes for superseded shader/cursor work"
      contains: "superseded"
    - path: ".env.example"
      provides: "Documented Web3Forms public key"
      contains: "NEXT_PUBLIC_WEB3FORMS_KEY"
---

<objective>
Reconcile the stale Phase 2 plan with the current Next.js site and remove the known visual build blockers around missing assets, zero-byte fonts, and undocumented environment variables.

Purpose: downstream work should not follow the old WebGL/custom-cursor direction or fail immediately because the hero references missing files.
Output: updated planning docs plus asset/font/env corrections that make the visual layer safe to build on.
</objective>

<context>
@.planning/phases/02-visual-layer/02-CONTEXT.md
@.planning/phases/02-visual-layer/02-RESEARCH.md
@.planning/phases/02-visual-layer/02-UI-SPEC.md
@.planning/codebase/CONCERNS.md
@public/README.md
</context>

<threat_model>
## Threat Model

- T-02-01: Accidentally expose a private key while documenting env vars.
  - Mitigation: only add empty example placeholders to `.env.example`.
- T-02-02: Break user work by replacing symlinked or typechanged assets blindly.
  - Mitigation: inspect file type and `git status` before editing; preserve unrelated changes.
- T-02-03: Reintroduce slow cursor/shader behavior by following stale plans.
  - Mitigation: codify supersession in planning docs before implementation continues.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Update planning docs to codify the visual pivot</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
    Update the planning docs so Phase 2 is no longer described as an SDF shader/custom cursor phase.

    Required edits:
    - In `.planning/PROJECT.md`, update active visual requirements to the current paper-brand system from `public/README.md`.
    - In `.planning/ROADMAP.md`, change Phase 2 goal and success criteria to "buildable paper-brand visual layer".
    - In `.planning/ROADMAP.md`, mark shader/cursor implementation as superseded by the pivot, not pending implementation.
    - In `.planning/REQUIREMENTS.md`, add a short note that SHDR-* and CURS-* are superseded for v1 by the paper-brand performance pivot.
    - Do not delete historical requirements unless the user explicitly asks.
  </action>
  <verify>
    <automated>Select-String -Path .planning/ROADMAP.md,.planning/REQUIREMENTS.md -Pattern 'superseded|paper-brand|paper brand' -CaseSensitive:$false</automated>
  </verify>
  <done>Planning docs explain that Phase 2 follows the current Next paper-brand direction and does not implement shader/cursor work.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve missing brand mark reference safely</name>
  <files>src/components/HeroSection.tsx, public/assets/logo-mark.svg</files>
  <action>
    Inspect `public/assets/` and `src/components/HeroSection.tsx`.

    If a valid brand mark SVG exists under another filename, update `brandMarkSrc` to that valid path.
    If no valid mark exists, create `public/assets/logo-mark.svg` from the existing canonical SVG asset with the smallest faithful markup.

    Do not use zero-byte PNG files. Do not introduce remote image dependencies.
  </action>
  <verify>
    <automated>Test-Path public/assets/logo-mark.svg; Select-String -Path src/components/HeroSection.tsx -Pattern 'logo-mark|brandMarkSrc'</automated>
  </verify>
  <done>The hero brand image points to an existing non-empty asset.</done>
</task>

<task type="auto">
  <name>Task 3: Make font loading robust</name>
  <files>src/app/layout.tsx, public/fonts/Fraunces-VariableFont_SOFT_WONK_opsz_wght.ttf, public/fonts/Fraunces-Italic-VariableFont_SOFT_WONK_opsz_wght.ttf</files>
  <action>
    Inspect the two Fraunces font files.

    If valid non-empty font files are available, keep `next/font/local`.
    If they are zero-byte or broken, change `src/app/layout.tsx` to avoid loading broken local files. Use a safe fallback variable for `--font-fraunces` so existing CSS continues to resolve.

    Keep DM Sans and JetBrains Mono via `next/font/google`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Next build no longer fails because of zero-byte local font files.</done>
</task>

<task type="auto">
  <name>Task 4: Document lead capture environment variable</name>
  <files>.env.example</files>
  <action>
    Add `NEXT_PUBLIC_WEB3FORMS_KEY=` to `.env.example` under public client variables or lead capture settings.
    Keep it empty. Do not add a real key.
  </action>
  <verify>
    <automated>Select-String -Path .env.example -Pattern '^NEXT_PUBLIC_WEB3FORMS_KEY='</automated>
  </verify>
  <done>Developers can see the required Web3Forms key without exposing credentials.</done>
</task>

</tasks>

<verification>
- Run `npm run lint`.
- Run `npm run build`.
- If build fails for unrelated dirty symlink/assets, document exact failing path in the summary.
</verification>

<success_criteria>
- Phase 2 docs no longer instruct agents to build the old shader/cursor direction.
- Hero brand asset path is valid.
- Broken local font loading is resolved or safely bypassed.
- `.env.example` documents `NEXT_PUBLIC_WEB3FORMS_KEY`.
- No unrelated dirty worktree changes are reverted.
</success_criteria>
