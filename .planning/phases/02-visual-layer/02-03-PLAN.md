---
phase: 02-visual-layer
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - src/components/HeroSection.tsx
  - src/components/ChatInterface.tsx
  - src/app/globals.css
  - README.md
  - public/voice-feature/README.md
requirements: [LAND-01, LAND-02, LAND-03, LAND-04, LAND-05, LAND-06, LAND-07, LAND-08, LAND-09, RESP-01, RESP-03]
autonomous: true

must_haves:
  truths:
    - "Desktop and mobile first viewports have no overlapping text or controls"
    - "No hidden cursor, shader, or ambient cursor glow behavior is introduced"
    - "The stray voice prototype is clearly marked as non-production or moved out of the public path"
    - "README reflects the current Next site enough for the next agent to start safely"
    - "Lint and build are run, or exact blockers are documented"
  artifacts:
    - path: "README.md"
      provides: "Current project setup and product direction"
      contains: "Next.js"
    - path: "src/app/globals.css"
      provides: "Reduced-motion and responsive safety styles"
      contains: "prefers-reduced-motion"
---

<objective>
Polish the recovered visual layer across responsive states, clarify non-production artifacts, and leave the phase ready for execution verification.

Purpose: after the site compiles, the remaining Phase 2 work is reducing confusion and making the current visual direction stable enough for chat/lead phases to build on.
Output: responsive polish, README cleanup, public prototype labeling or relocation, and final lint/build verification.
</objective>

<context>
@.planning/phases/02-visual-layer/02-CONTEXT.md
@.planning/phases/02-visual-layer/02-UI-SPEC.md
@.planning/phases/02-visual-layer/02-VALIDATION.md
@.planning/codebase/TESTING.md
@public/README.md
</context>

<threat_model>
## Threat Model

- T-02-07: Prototype code under `public/` is mistaken for production architecture.
  - Mitigation: mark it clearly or move it only if safe and low-churn.
- T-02-08: Responsive polish breaks desktop or mobile layout.
  - Mitigation: verify both viewport families and keep layout constraints stable.
- T-02-09: README exposes local machine paths or stale secrets guidance.
  - Mitigation: keep docs generic and do not include private env values.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Responsive visual pass</name>
  <files>src/components/HeroSection.tsx, src/components/ChatInterface.tsx, src/app/globals.css</files>
  <action>
    Review desktop and mobile layout constraints.

    Required:
    - No horizontal scroll under 390px width.
    - Chat shell input and buttons keep stable dimensions.
    - Feature chips wrap cleanly.
    - Footer text does not collide on mobile.
    - Reduced-motion rules cover marquee/typing animation if needed.

    Keep changes scoped to layout safety. Do not redesign the whole page.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Responsive layout has stable constraints and reduced-motion coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Clarify or quarantine the voice prototype</name>
  <files>public/voice-feature/README.md, README.md</files>
  <action>
    Make it explicit that `public/voice-feature/` is not the production Next site.

    Prefer a low-churn documentation change:
    - Add a top warning to `public/voice-feature/README.md` if the file exists.
    - Add a short note in root `README.md` that production work happens under `src/`.

    Only move the folder out of `public/` if doing so is clearly safe and does not cause large unrelated churn.
  </action>
  <verify>
    <automated>Select-String -Path README.md,public/voice-feature/README.md -Pattern 'not production|prototype|src/' -CaseSensitive:$false</automated>
  </verify>
  <done>The prototype cannot easily be mistaken for the active app direction.</done>
</task>

<task type="auto">
  <name>Task 3: Final visual layer verification</name>
  <files>src/components/HeroSection.tsx, src/components/ChatInterface.tsx, src/app/globals.css</files>
  <action>
    Run the phase validation commands.

    If both commands pass, start the dev server and manually inspect:
    - desktop first viewport
    - mobile first viewport
    - initial chat submit
    - lead card appearance if the first response path is available

    If a command fails because of unrelated dirty symlinks or external asset state, record the exact blocker in the summary and do not paper over it.
  </action>
  <verify>
    <automated>npm run lint; npm run build</automated>
  </verify>
  <done>Phase 2 has a clear pass/fail state with command output summarized in the execution summary.</done>
</task>

</tasks>

<verification>
- `npm run lint`
- `npm run build`
- Manual desktop/mobile browser inspection when the build passes.
</verification>

<success_criteria>
- No visual code reintroduces the old shader/cursor plan.
- The page is stable on desktop and mobile.
- The prototype folder is clearly labeled as non-production.
- README no longer reads like an untouched create-next-app scaffold.
- Phase 3 can proceed with chat behavior rather than visual recovery.
</success_criteria>
