---
phase: 02-visual-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/LandingHero.tsx
  - src/components/NoiseOverlay.tsx
  - src/app/page.tsx
  - src/app/globals.css
autonomous: true
requirements: [LAND-01, LAND-02, LAND-03, LAND-04, LAND-05, LAND-06, LAND-07, LAND-08, LAND-09]

must_haves:
  truths:
    - "Wordmark 'Good'ai' displays centered with copper apostrophe at 0.7 opacity"
    - "Subtitle 'business automations, sorted' renders in JetBrains Mono below wordmark"
    - "Input field with placeholder 'Tell us your problem.' is centered with submit arrow"
    - "Hint text 'We'll figure out how to fix it.' appears below input"
    - "Perth badge 'PERTH / WA' is fixed at bottom center"
    - "All elements stagger fade-in on load with correct delays"
    - "Noise texture overlay and vignette are visible"
  artifacts:
    - path: "src/components/LandingHero.tsx"
      provides: "Landing page content — wordmark, subtitle, input, hint, badge"
      contains: "'use client'"
      min_lines: 60
    - path: "src/components/NoiseOverlay.tsx"
      provides: "Noise texture overlay and vignette overlays"
      min_lines: 15
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/LandingHero.tsx"
      via: "import and render"
      pattern: "LandingHero"
    - from: "src/app/page.tsx"
      to: "src/components/NoiseOverlay.tsx"
      via: "import and render"
      pattern: "NoiseOverlay"
    - from: "src/components/LandingHero.tsx"
      to: "src/components/ui/input.tsx"
      via: "shadcn Input for text field"
      pattern: "import.*Input.*from.*ui/input"
---

<objective>
Build the landing page UI — wordmark, subtitle, input field, hint text, Perth badge, noise overlay, and vignette — with staggered fade-in animations.

Purpose: The landing state is the first thing visitors see. It must communicate "Good'ai = premium business automations in Perth" within 3 seconds.
Output: LandingHero and NoiseOverlay components, CSS animations, page.tsx wiring.
</objective>

<execution_context>
@C:/Users/kevin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kevin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@inbox/app-spec-ui.md

<interfaces>
<!-- Phase 1 provides design tokens and fonts -->
From src/app/globals.css (expected from Phase 1):
- CSS custom properties: --bg (#0C0C0C), --surface (#151514), --surface-raised (#1C1C1A),
  --border (#2A2925), --border-hover (#3D3A35), --text (#D7D2CB), --text-bright (#EDE9E3),
  --text-dim (#6B6560), --accent (#D86A3D), --accent-soft (#C4714D), --accent-dim (rgba(216,106,61,0.08)),
  --accent-glow (rgba(216,106,61,0.04))

From src/app/layout.tsx (expected from Phase 1):
- DM Sans loaded via next/font as primary font
- JetBrains Mono loaded via next/font as mono font
- Font CSS variables applied to html element (e.g., --font-sans, --font-mono)

From src/components/ui/input.tsx (shadcn, pre-installed):
- Input component with standard props (placeholder, value, onChange, className, etc.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LandingHero component with all landing UI elements</name>
  <files>src/components/LandingHero.tsx, src/app/globals.css</files>
  <action>
    **LandingHero.tsx** — Create a `'use client'` component with the full landing state layout:

    1. **Structure (single centered column, full viewport):**
       ```
       <main> — flex column, items-center, justify-center, min-h-screen, relative z-10, px-10 (40px)
         <h1> wordmark — "Good'ai" with <span> copper apostrophe
         <p> subtitle — "business automations, sorted"
         <div> input-area — max-w-[540px] w-full
           <form> — relative
             <Input> — shadcn, placeholder "Tell us your problem."
             <button> — absolute right-2, submit arrow SVG
           </form>
           <p> hint — "We'll figure out how to fix it."
         </div>
         <div> badge — fixed bottom-7, "PERTH" <span>/</span> "WA"
       </main>
       ```

    2. **Wordmark styling:**
       - `text-[34px] font-bold tracking-[-0.04em]` using `text-[var(--text-bright)]`
       - Apostrophe `<span>` with `text-[var(--accent)] opacity-70`
       - `mb-14` (56px margin-bottom)
       - Animation class: `animate-fadeUp` with `animation-delay: 0.3s`, `opacity-0` initially (fill forwards)

    3. **Subtitle styling:**
       - `font-mono text-[11px] font-light tracking-[0.14em] uppercase text-[var(--text-dim)]`
       - `-mt-10 mb-14` (pulls up toward wordmark, 56px below)
       - Animation: `animate-fadeUp` delay `0.55s`

    4. **Input field:**
       - Use shadcn `<Input>` component with className overrides:
         - `w-full py-5 pl-6 pr-14 bg-[var(--surface)] border-[var(--border)] rounded-[14px] text-base font-normal text-[var(--text-bright)] placeholder:text-[var(--text-dim)] placeholder:font-light focus:border-[rgba(216,106,61,0.25)] focus:shadow-[0_0_4px_var(--accent-dim),0_0_32px_rgba(0,0,0,0.5)] focus:bg-[var(--surface-raised)] cursor-none`
       - Placeholder: `"Tell us your problem."`
       - `onSubmit` on form: prevent default, do nothing for now (Phase 3 wires chat)

    5. **Submit button (inside form, after input):**
       - `absolute right-2 top-1/2 -translate-y-1/2 w-10 h-10 rounded-[10px] bg-transparent border-0 text-[var(--text-dim)] hover:text-[var(--accent)] hover:bg-[var(--accent-dim)] cursor-none transition-colors duration-300`
       - SVG arrow icon: `<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>`
       - `type="submit"`

    6. **Hint text:**
       - `"We'll figure out how to fix it."`
       - `mt-4 font-mono text-xs font-light tracking-[0.04em] text-[var(--text-dim)] opacity-70`

    7. **Input area wrapper:**
       - `max-w-[540px] w-full` with animation: `animate-fadeUp` delay `0.7s`

    8. **Perth badge:**
       - `fixed bottom-7 left-1/2 -translate-x-1/2 font-mono text-[10px] font-light tracking-[0.12em] uppercase text-[var(--text-dim)]`
       - Content: `PERTH <span className="text-[var(--accent-soft)] opacity-50">/</span> WA`
       - Animation: `animate-fadeUp` delay `1.2s`

    9. **Form currently non-functional** — Phase 3 (Chat Core) will add the `onSubmit` handler that transitions to conversation mode. For now, the form just prevents default on submit.

    **globals.css additions** — Add the `fadeUp` keyframe and animation utility:
    ```css
    @keyframes fadeUp {
      from {
        opacity: 0;
        transform: translateY(14px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .animate-fadeUp {
      animation: fadeUp 1s cubic-bezier(0.23, 1, 0.32, 1) both;
    }
    ```

    Each animated element starts with `opacity-0` and uses inline `style={{ animationDelay: 'Xs' }}` for stagger timing.
  </action>
  <verify>
    <automated>cd "C:/Users/kevin/knowledge2026/Projects-Coding/nextjs" && npx next build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Wordmark "Good'ai" displays centered with copper apostrophe at 0.7 opacity
    - Subtitle in JetBrains Mono below wordmark
    - Input field with placeholder and arrow submit button
    - Hint text below input
    - Perth badge fixed at bottom center with copper slash
    - All elements fade in with correct staggered delays (0.3s, 0.55s, 0.7s, 1.2s)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create NoiseOverlay and Vignette, wire all into page.tsx</name>
  <files>src/components/NoiseOverlay.tsx, src/app/page.tsx</files>
  <action>
    **NoiseOverlay.tsx** — Create a component that renders both noise texture and vignette as fixed overlays:

    1. **Noise texture** — Use an SVG feTurbulence filter encoded as a data URI for the background:
       ```tsx
       <div
         className="fixed inset-0 z-[9998] pointer-events-none opacity-[0.028]"
         style={{
           backgroundImage: `url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.85' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E")`,
           backgroundSize: '200px 200px',
         }}
         aria-hidden="true"
       />
       ```

    2. **Vignette** — Radial gradient, transparent center to dark edges:
       ```tsx
       <div
         className="fixed inset-0 z-0 pointer-events-none"
         style={{
           background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.4) 100%)',
         }}
         aria-hidden="true"
       />
       ```

    3. Export as single default component rendering both divs.

    **page.tsx** — Wire LandingHero and NoiseOverlay alongside ShaderBackground (from Plan 01):

    ```tsx
    import dynamic from 'next/dynamic';
    import LandingHero from '@/components/LandingHero';
    import NoiseOverlay from '@/components/NoiseOverlay';

    const ShaderBackground = dynamic(() => import('@/components/ShaderBackground'), {
      ssr: false,
      loading: () => <div className="fixed inset-0 -z-10 bg-[var(--bg)]" />,
    });

    export default function Home() {
      return (
        <>
          <ShaderBackground />
          <NoiseOverlay />
          <LandingHero />
        </>
      );
    }
    ```

    Note: If Plan 01 hasn't run yet, the ShaderBackground import will be missing — that's fine. Add a conditional comment or just include the import. Both plans target page.tsx but Plan 01 only adds ShaderBackground and Plan 02 adds LandingHero + NoiseOverlay. The final page.tsx should contain all three. Since both plans are wave 1, the executor that runs second should merge, not overwrite.

    To handle this cleanly: write page.tsx with all three imports. If ShaderBackground doesn't exist yet, the build will fail but dev will work with the other two components. The second plan to execute will fix it.
  </action>
  <verify>
    <automated>cd "C:/Users/kevin/knowledge2026/Projects-Coding/nextjs" && test -f src/components/NoiseOverlay.tsx && test -f src/components/LandingHero.tsx && grep -q "NoiseOverlay" src/app/page.tsx && grep -q "LandingHero" src/app/page.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - Noise texture overlay visible with subtle grain (0.028 opacity)
    - Vignette darkens edges, focuses attention on center content
    - page.tsx renders ShaderBackground + NoiseOverlay + LandingHero in correct z-order
    - Neither overlay blocks pointer events
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` — landing page shows wordmark, subtitle, input, hint, badge with staggered animations
2. Noise texture visible as subtle grain over the entire viewport
3. Vignette visible as darkened edges
4. Input field accepts text, placeholder displays correctly
5. Submit button shows arrow, hover changes to copper
6. Perth badge fixed at bottom center
7. All text uses correct fonts (DM Sans body, JetBrains Mono for mono elements)
</verification>

<success_criteria>
- All 9 LAND requirements visually verifiable on the landing page
- Staggered fadeUp animations fire at 0.3s, 0.55s, 0.7s, 1.2s delays
- Noise + vignette overlays render without blocking interaction
- Input field uses shadcn Input component
- Correct design tokens applied (colors, fonts, spacing match spec)
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-layer/02-02-SUMMARY.md`
</output>
